# Optional. A GitHub token raises the GitHub API rate limit from 60 to 5000 requests/hour.
# Create one at https://github.com/settings/tokens — no scopes are needed for public data.
DEPWATCH_GITHUB_TOKEN=
